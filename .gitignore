# Build output — regenerated by `npm run build`, never committed.
dist/

# Dependencies
node_modules/

# Local scratch and editor noise
*.log
.DS_Store
.vscode/
.idea/
*.swp

# Nothing secret lives in this repo today: the site is static, has no API keys and no
# backend. These are here so that stays true by default rather than by memory.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keystore
*.jks
secrets.json
service-account*.json
